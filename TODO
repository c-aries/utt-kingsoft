	-*- outline -*-
* article training
** learn cairo, cairo_show_text(), learn freetype2, how bbox was generated?
<code>
scale = face->size->metrics.y_scale;
ascent = FT_CEIL(FT_MulFix(face->bbox.yMax, scale));
descent = FT_CEIL(FT_MulFix(face->bbox.yMin, scale));
</code>
We can learn it from GtkTextView actually.
** wubi input method
** UttArticle read training files and Layout
** Training Begin
** desktop.in
* word training
* key training
